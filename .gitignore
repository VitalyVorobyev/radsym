/target
Cargo.lock
*.swp
*.swo
*~
.DS_Store
data
.venv
output
__pycache__
*.pyc
.pytest_cache
book/book
# mdBook renders the "API Documentation (rustdoc)" SUMMARY entry (a relative
# link to ../api/radsym/index.html) outside its build dir into book/api/. It is
# regenerated on every build and never deployed — the Pages workflow serves
# fresh `cargo doc` output at /api/ instead.
book/api
# book/src/demo/ is fully generated by book/build.sh from the canonical demo/
# source (demo files + built pkg + sample images); never committed.
book/src/demo/
# Local-dev WASM build placed next to the standalone demo (built on demand).
demo/pkg/
_site
API_REVISION.md